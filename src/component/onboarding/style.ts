import {styled} from 'nativewind';
import {Text, View} from 'react-native';

export const TextContainer = styled(
  Text,
  'mb-[38px] text-[32px] text-white font-[500] leading-[32px]',
);
export const StyledView = styled(View, 'flex items-center justify-center');
export const StyledText = styled(
  Text,
  ' text-center text-[26px] leading-normal text-textwhite font-[600] font-poppings',
);
export const ButtomTextContainer = styled(
  Text,
  'mt-7 text-[i4px] text-textgrey font-normal leading-[14px]',
);
